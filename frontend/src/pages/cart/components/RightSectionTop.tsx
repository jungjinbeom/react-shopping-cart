import { PropsWithChildren } from "react";

const RightSectionTop = ({ children }: PropsWithChildren) => {
  return <div className="cart-right-section__top">{children}</div>;
};

export default RightSectionTop;
